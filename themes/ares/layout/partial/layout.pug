doctype
html(lang=page.lang || config.language[0])
    head
        include head
    body
        .wrap
            header
                a.logo-link(href=url_for())
                    img(src=url_for(theme.logo) alt="logo")
                include nav
            main.container
                block container
            footer
                block pagination
                block copyright
        include code
        include scripts
        include search